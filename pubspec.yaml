name: ongkir
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  dropdown_search: ^6.0.2
  flutter:
    sdk: flutter
  flutter_dotenv: ^6.0.0
  get: ^4.7.2
  http: ^1.5.0
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
